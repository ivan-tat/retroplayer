; s3mtypes.def -- Assembler declarations for s3mtypes.h/s3mtypes.pas.
;
; This is free and unencumbered software released into the public domain.
; For more information, please refer to <http://unlicense.org>.

ifndef S3MTYPES_DEF
S3MTYPES_DEF equ 1

TCHANNEL STRUC
    bEnabled        db ?
    bChannelType    db ?
    wInsSeg         dw ?
    wSmpSeg         dw ?
    bIns            db ?
    bNote           db ?
    bSmpVol         db ?
    bSmpFlags       db ?
    wSmpStart       dw ?
    wSmpLoopStart   dw ?
    wSmpLoopEnd     dw ?
    dSmpPos         dd ?
    dSmpStep        dd ?
    wSmpPeriod      dw ?
    wSmpPeriodLow   dw ?
    wSmpPeriodHigh  dw ?
    wCommand        dw ?
    wCommand2       dw ?
    bParameter      db ?
    bEffFlags       db ?
    wVibTab         dw ?
    wTrmTab         dw ?
    bTabPos         db ?
    bVibParam       db ?
    bPortParam      db ?
    wSmpPeriodOld   dw ?
    bSmpVolOld      db ?
    wSmpPeriodDest  dw ?
    bArpPos         db ?
    bArpNotes       db 2 dup (?)
    dArpSmpSteps    dd 3 dup (?)
    bRetrigTicks    db ?
    bSavNote        db ?
    bSavIns         db ?
    bSavVol         db ?
    bDelayTicks     db ?
TCHANNEL ENDS

TINSTRUMENT STRUC
    TYP             db ?
    DOSname         db 12 dup (?)
    notused1        db ?
    memseg          dw ?
    slength         dw ?
    HI_length       dw ?
    loopbeg         dw ?
    HI_loopbeg      dw ?
    loopend         dw ?
    HI_loopend      dw ?
    vol             db ?
    notused2        db ?
    packinfo        db ?
    flags           db ?
    c2speed         dw ?
    HI_c2speed      dw ?
    notused3        db 4 dup (?)
    GUS_addr        dw ?
    SB_Flags        dw ?
    SB_last         dd ?
    IName           db 28 dup (?)
    SCRS_ID         db 4 dup (?)
TINSTRUMENT ENDS

endif   ; S3MTYPES_DEF
