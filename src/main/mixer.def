; mixer.def -- Assembler declarations for "mixer" library.
;
; This is free and unencumbered software released into the public domain.
; For more information, please refer to <http://unlicense.org>.

ifndef MIXER_DEF
MIXER_DEF equ 1

include "mixer_.def"

_DATA segment word public use16 'DATA'

extrn ST3Periods: word

extrn mixBuf: dword
extrn mixSampleRate: word
extrn mixChannels: byte
extrn mixBufSamplesPerChannel: word
extrn mixBufSamples: word
extrn mixTickSamplesPerChannel: word
extrn mixTickSamplesPerChannelLeft: word

_DATA ends

MIXER_TEXT segment word public use16 'CODE'

extrn _calc_sample_step: far

extrn setMixSampleRate: far
extrn setMixChannels: far
extrn setMixBufSamplesPerChannel: far
extrn setMixMode: far
extrn getMixBufOffFromCount: far
extrn getCountFromMixBufOff: far

MIXER_TEXT ends

endif   ; MIXER_DEF
